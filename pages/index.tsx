import LoggedInView from '../components/LoggedInView';

const Home = () => {
  if (false) {
    return <LoggedInView />;
  }
  return <div style={{ color: 'white' }}>Home Page</div>;
};

export default Home;
